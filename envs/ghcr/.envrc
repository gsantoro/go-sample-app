dotenv ../docker/.env
dotenv .env.private
dotenv

env_vars_required DOCKER_PASSWORD # defined in .env.private

../../bin/show-direnv-diff.sh | jq .n | jq 'del(.DIRENV_DIR)' | jq 'del(.DIRENV_FILE)' | jq 'del(.DIRENV_WATCHES)'